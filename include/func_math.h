#ifndef FUNC_MATH_H
#define FUNC_MATH_H

#include <iostream>
#include <vector>

using namespace std;

int summation(vector<int> numbers);
float average(vector<int> numbers);
float calc_func(float n);

#endif